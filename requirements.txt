PySide6==6.9.0           # GUI framework for Aurora
selenium==4.31.0         # Browser control and automation
pillow                   # Not listed, check version if used (optional)
lxml==5.3.2              # XML/HTML parser (used in planned modules)
requests                 # Standard HTTP lib (not versioned yet, check if critical)
beautifulsoup4==4.13.3   # Screen-scraping + HTML cleanup routines
# sqlite3 is part of the Python standard library and does not need to be installed.
